kind: DataContract
apiVersion: odcs/v3
id: bronze_gtr_organizations_raw
status: active
owner:
  name: local_data_platform
  contact: data-platform@example.com
dataset:
  name: bronze_gtr_organizations_raw
  description: Raw organisation records from UKRI Gateway to Research API v2.
  schema:
    - name: organization_id
      logicalType: string
      required: true
      unique: true
    - name: ingested_at
      logicalType: timestamp
      required: true
    - name: payload
      logicalType: object
      required: true
