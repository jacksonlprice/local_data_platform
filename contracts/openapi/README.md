# OpenAPI contracts

This directory contains source API contract material for the data platform.

- `gtrapi2.minimal.yaml` captures the minimum UKRI Gateway to Research API v2 paths used in the first thin-slice product (`projects` and `organisations`).
